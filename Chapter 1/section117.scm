#|
sqrt of x = the y such that y >= 0 and y2 = x

in psuedo code

(define (sqrt x)
    (the y (and (>= y 0)
            (= (square y) x))))
        

the mathematical definition does not define a procedure and as such we cannot use it.o

